import { EldirDirective } from './eldir.directive';

describe('EldirDirective', () => {
  it('should create an instance', () => {
    const directive = new EldirDirective();
    expect(directive).toBeTruthy();
  });
});
